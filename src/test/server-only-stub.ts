/* Stub de "server-only" pour les tests Vitest (environnement Node pur).
   Le vrai paquet jette une erreur quand il est importé hors d'un
   React Server Component — inutile dans les tests unitaires. */
export {};
